/**
 * Supabase server client for Server Components / Route Handlers.
 * TODO: Create server client with createServerClient from @supabase/ssr, using cookies().
 * TODO: Use for fetching session and profiles.role in dashboard.
 */
export function createClient() {
  // TODO: return createServerClient(...) with cookie storage
  throw new Error("Not implemented: create Supabase server client");
}
