/**
 * Supabase browser client.
 * TODO: Create SupabaseClient with NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY.
 * TODO: Use createBrowserClient from @supabase/ssr when implementing auth.
 */
export function createClient() {
  // TODO: return createBrowserClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!);
  throw new Error("Not implemented: create Supabase browser client");
}
