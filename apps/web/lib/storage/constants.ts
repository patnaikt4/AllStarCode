// Storage bucket name constants — import these wherever you reference Supabase Storage
// so the bucket name is defined in exactly one place and stays in sync across routes.

/** Bucket that holds instructor-uploaded lesson plan PDFs */
export const LESSON_PLANS_BUCKET = 'lesson-plans'

/** Bucket that holds AI-generated feedback PDFs */
export const FEEDBACK_BUCKET = 'feedback-pdfs'
